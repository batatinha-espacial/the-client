# HTML Global Attribute - inert

The inert global boolean attribute makes the browser ignore input from the element and it's decendants.<br>
It makes the browser to not fire any input events to the element, and removes it from assistive technologies.